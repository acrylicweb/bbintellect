const IndividualDevotional = ({ releasedate, title, fullentry }) => {
  return (
    <div className="individual-devotional-container">
      <div className="text">{fullentry}</div>
    </div>
  );
};

export default IndividualDevotional;
